<br>
<h2 class="text-warning">以下の内容で登録してよろしいですか？</h1>
<br>
<%= form_with(model: @user, local: true) do |f| %>
  <%= f.hidden_field :user_name %>
  <%= f.hidden_field :email %>
  <%= f.hidden_field :password %>
  <%= f.hidden_field :password_confirmation %>
  
  <div class="card">
    <div class="card-header">
      ユーザーネーム
    </div>
    <div class="card-body">
      <h5 class="card-title"><%= @user.user_name %></h5>
    </div>
  </div>

  <div class="card">
    <div class="card-header">
      メールアドレス
    </div>
    <div class="card-body">
      <h5 class="card-title"><%= @user.email %></h5>
    </div>
  </div>
  
    <div class="card">
    <div class="card-header">
      パスワード
    </div>
    <div class="card-body">
      <p class="card-text">セキュリティ保護のため表示されません。</p>
    </div>
  </div>
  <br>
  <%= f.submit 'この内容で登録する', class: "btn btn-primary" %>
<% end %>
<br>
<%= form_with(model: @user, local: true, url: new_user_path, method: 'get') do |f| %>
  <%= f.hidden_field :user_name %>
  <%= f.hidden_field :email %>
  
  <%= f.submit '戻る', name: 'back', class: "btn tbn-light" %>
<% end %>
